services:

- type: web
  name: rss-feed-api
  runtime: python3
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn –bind 0.0.0.0:$PORT app:app
  plan: free
  env:
  - key: PYTHON_VERSION
    value: 3.9.16
